## What this changes

<!-- What behaviour is different afterwards, and what problem it solves. -->

## How it was checked

<!-- Which formats and which real files. "Converted three PS2 ISOs, NetherSX2
     still loads them" is worth more than "tests pass". -->

- [ ] `pytest` passes and `ruff check .` is clean
- [ ] Tested against a real ROM, if this touches a converter
